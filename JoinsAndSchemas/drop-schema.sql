
-- drop schema <schema-name> cascade; -- This will drop the schema along with all its tables and data.
-- drop schema <schema-name> restrict; -- This will fail if the schema is not empty. You need to drop the tables first or use cascade.